My Goal
I want you to analyze my .NET 10 boilerplate solution and generate a Reusable Agent Skill (comprising an AI Prompt definition and execution scripts) that can replicate this exact solution dynamically. When I provide a ProjectName, the scripts must automatically:

Create the .slnx file using .NET 10 CLI.
Create the 8 projects:
{ProjectName}.Domain
{ProjectName}.Domain.Tests
{ProjectName}.Application
{ProjectName}.Application.Tests
{ProjectName}.Infrastructure
{ProjectName}.Infrastructure.Tests
{ProjectName}.API
{ProjectName}.API.Tests
Add them to the .slnx file.
Set up the exact correct project-to-project references.
Recreate every folder and file from my boilerplate, dynamically replacing the old root namespace (e.g., UMS) with the new {ProjectName} namespace in all .cs and .csproj files.
Restore the exact NuGet packages for each project.
CRITICAL RULE: 100% Self-Contained Scripts
The generated PowerShell (Scaffold.ps1) and Bash (Scaffold.sh) scripts MUST NOT rely on reading any external template files. You must embed the content of every single .cs, .csproj, and config file directly into the scripts using single-quoted here-strings (PowerShell @' ... '@) and quoted heredocs (Bash << 'EOF'). The script alone must be sufficient to generate the entire solution from scratch on any machine.

Phase 1: Analyze My Boilerplate
I will provide you with my boilerplate code, folder structure, .slnx content, .csproj files, and source code. Your first task is to map out:

The exact folder tree for each of the 8 projects.
Every NuGet package and version per .csproj.
The exact project reference chains (including Test project references).
All boilerplate code, noting the exact lines where the root namespace UMS is used (so we know what to replace later).
Phase 2: Generate the Agent Skill Definition
Create a .md prompt file that I can save as an Agent Skill in VS Code (for tools like GitHub Copilot Chat, Cline, or Continue.dev). This prompt must instruct the AI agent on exactly how to scaffold the solution when invoked. It must include:

Input Parameter Handling: Ask the user for ProjectName.
Execution Instructions: Step-by-step directions for the AI to create directories, write files, and run CLI commands (dotnet new sln -n, dotnet new classlib, dotnet new xunit, dotnet sln add, etc.).
Templating Rules: Explicit instructions to take the embedded boilerplate code and replace the old namespace (UMS) with the new namespace ({ProjectName}).
Phase 3: Generate the Master Scaffolding Scripts
To make this skill bulletproof, generate a PowerShell script (Scaffold.ps1) and a Bash script (Scaffold.sh) that the Agent Skill can execute directly.

The scripts must:

Accept a -ProjectName (or --ProjectName) parameter.
Create the directory structure dynamically.
Run the dotnet new commands to create the classlib, webapi, and xunit test projects.
Create the .slnx using dotnet new sln (which defaults to .slnx in .NET 10) and add all 8 projects.
Run dotnet add reference to link the projects correctly based on Clean Architecture standards:
Domain has no references.
Application references Domain.
Infrastructure references Application (and Domain if explicitly mapped).
API references Application and Infrastructure.
Domain.Tests references Domain.
Application.Tests references Application.
Infrastructure.Tests references Infrastructure.
API.Tests references API.
Run dotnet add package for every NuGet required.
Embed and write out ALL boilerplate C# files dynamically, using string replacement to ensure the namespaces match the -ProjectName. Use single-quoted here-strings/heredocs to prevent variable interpolation issues.
How We Will Work
I will now provide you with my boilerplate structure and code. Please acknowledge that you understand the mission. Then, as I paste the files, build the mapping. Once I say "GENERATE", you will output:

The Agent Skill Prompt Definition (.md).
The Scaffold.ps1 script.
The Scaffold.sh script.
