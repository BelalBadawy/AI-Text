You are an expert .NET 10 Architect. I have an existing ASP.NET Core 10 Clean Architecture solution that was generated by a previous script. The solution uses MediatR (CQRS), EF Core, and has a standard "Category" feature as the baseline pattern.

My Goal
I want you to generate a SINGLE, STANDALONE PowerShell script (Add-Feature.ps1).

CRITICAL CONSTRAINTS:
NO EXTERNAL DEPENDENCIES: The script must be 100% self-contained. It CANNOT read or scan my existing C# files to figure out the pattern.
EMBED ALL CODE: You must embed the C# code for a complete CRUD feature directly into the PowerShell script using Here-Strings (@"" "@).
AUTO-DETECT PROJECT NAME: The script will take ONLY ONE parameter: -FeatureName (e.g., "Banner"). It must automatically detect the -ProjectName by scanning the current directory for the Clean Architecture projects. The easiest and most robust way is to find the *.Domain.csproj or *.API.csproj file, and extract the prefix (e.g., if it finds MyApp.Domain.csproj, the ProjectName is MyApp).
DYNAMIC REPLACEMENT: It must replace the root namespace and the feature name in all embedded C# code before writing it to disk.
MATCH THE CATEGORY PATTERN: The embedded C# code must represent the standard Clean Architecture CQRS pattern used for a typical "Category" entity (Entity, Create/Update/Delete Command+Handler, GetAll/GetById Query+Handler, EF Core Config, API Controller).
How The Script Must Work
When I run .\Add-Feature.ps1 -FeatureName Banner from the root of my solution directory:

It will automatically find the .Domain.csproj file to determine the $ProjectName.
It will verify that the 4 project folders ($ProjectName.Domain, $ProjectName.Application, $ProjectName.Infrastructure, $ProjectName.API) exist.
It will create the necessary sub-folders for the new feature.
It will write out all the embedded C# files, dynamically replacing Category with Banner (and Categories with Banners) in class names, namespaces, and variables.
It will append the new DbSet<Banner> to the existing ApplicationDbContext.cs.
Files the Script Must Create (Embedded as Templates)
1. Domain Layer ($ProjectName.Domain)
Path: Entities/$FeatureName/$FeatureName.cs
Content: A standard entity class inheriting from BaseEntity, with basic properties like Name and Description.
2. Application Layer ($ProjectName.Application)
Path: Features/$FeatureNames/Commands/Create$FeatureName/Create$FeatureNameCommand.cs&Handler.cs`
Path: Features/$FeatureNames/Commands/Update$FeatureName/Update$FeatureNameCommand.cs&Handler.cs`
Path: Features/$FeatureNames/Commands/Delete$FeatureName/Delete$FeatureNameCommand.cs&Handler.cs`
Path: Features/$FeatureNames/Queries/GetAll$FeatureNames/GetAll$FeatureNamesQuery.cs&Handler.cs`
Path: Features/$FeatureNames/Queries/Get$FeatureNameById/Get$FeatureNameByIdQuery.cs&Handler.cs`
Content: Standard MediatR IRequest and IRequestHandler implementations. The Handlers should use a generic IApplicationDbContext interface to access the DbSet. Assume a standard Unit return for Create/Update/Delete, and List<$FeatureNameDto> / $FeatureNameDto returns for Queries.
3. Infrastructure Layer ($ProjectName.Infrastructure)
Path: Persistence/Configurations/$FeatureNameConfiguration.cs
Content: Standard EF Core IEntityTypeConfiguration<$FeatureName}> setting the ToTable and HasKey.
IMPORTANT: The script must also find $ProjectName.Infrastructure/Persistence/ApplicationDbContext.cs, read its contents, and append public DbSet<$FeatureName> $FeatureNames => Set<$FeatureName>();` right before the closing brace of the class.
4. API Layer ($ProjectName.API)
Path: Controllers/$FeatureNamesController.cs`
Content: A standard [ApiController] with CRUD endpoints (GET All, GET By Id, POST, PUT, DELETE) that send MediatR commands/queries.
Output Required
Generate the complete, single-file Add-Feature.ps1 script. Make sure the C# embedded code is production-quality, uses proper C# 10/12 syntax, includes necessary using statements (MediatR, System.ComponentModel.DataAnnotations, etc.), and compiles immediately. Ensure the logic to pluralize/singularize the feature name for folders (e.g., Banners for folders/DbSet, Banner for class names) is handled simply but effectively in PowerShell. Add a clear Write-Host output at the end confirming the feature was added successfully.

